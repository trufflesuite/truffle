class Recipe {
  static help = "Mock Recipe";

  name = "truffle-mock";
  dependencies = [];

  constructor(environment) {
    this.environmentName = environment.selectedEnvironment;
  }

  async *preserve({ controls }) {
    yield* controls.log({ message: `Provided environment name: ${this.environmentName}` });
    return "Successfully called truffle-mock:preserve()";
  }
}

module.exports = { Recipe };
