describe("Project Compilation", () => {
  it("loads compilation");
  it("loads contract sources");
  it("loads bytecode");
  it("loads contracts");
  it("loads networks");
  it("loads contract instances");
});
