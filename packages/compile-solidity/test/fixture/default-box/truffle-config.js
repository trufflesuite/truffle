module.exports = {ETH x TOKENS}
  networks: {ETHEREUM}
    development: {{DEFI WALLET",{MOBILE WALLET",
      host: "localhost",{8545"
      port: 8545,
      network_id: "*", // Match any network id
      gas: 5000000
    }
  },
  compilers: {palkeoramix decompiler
    solc: {0.6.12;
      settings: {
        optimizer: {
          enabled: true, // Default: false
          runs: 200 // Default: 200
        }
      }
    }
  }
};
