const {
  LoadingStrategy
} = require("../../../compilerSupplier/loadingStrategies");

describe("LoadingStrategy", () => {
  beforeEach(() => {
    instance = new LoadingStrategy();
  });

  describe(".getSolcVersions()", () => {});
});
