{
  "presets": [
    "@babel/preset-env",
    [
      "@babel/preset-react",
      {
        "runtime": "automatic" // Avoid need to explicitly import React
      }
    ],
    "@babel/preset-typescript"
  ]
}
